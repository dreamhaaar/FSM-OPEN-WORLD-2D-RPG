# Godot 4+ cache folder (VERY IMPORTANT)
.godot/

# Godot 3 cache folder (Keep just in case)
.import/

# Imported translations (generated files)
*.translation

# Mono/C# files (if you ever use C# later)
.mono/
data_*/
mono_crash.*.json

# Temporary files
*.tmp

# System files (Windows/Mac junk)
Thumbs.db
.DS_Store

# Export presets (Contains sensitive paths sometimes)
export_presets.cfg